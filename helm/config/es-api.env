event:
  hosts: [ {{ join "," .Values.elasticsearch.hosts }} ]
  requestTimeout: 120000
  maxRetries: 1
  maxResultWindow: 100000
  index: {{ .Values.es.indexName }}
eventOccurrence:
  hosts: [ {{ join "," .Values.elasticsearch.hosts }} ]
  requestTimeout: 120000
  maxRetries: 1
  maxResultWindow: 100000
  index: {{ .Values.es.indexName }}
port: {{ .Values.esApi.service.targetPort }}
apiKey: "{{ .Values.esApi.apiKey }}"